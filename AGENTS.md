# Agent Instructions

- Do not manually edit generated cmdlet command documentation under `website/docs/commands/`, `website/versioned_docs/*/commands/`, or the generated command sidebar. Command docs are generated by the build/docs pipeline from the PowerShell source, so update the cmdlet source/comment-based help instead and let automation regenerate those files.
