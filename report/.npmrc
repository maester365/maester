# TEMPORARY WORKAROUND: @tremor/react declares peer dependency react@"^18.0.0"
# but works fine with React 19 at runtime. This setting bypasses the peer dependency
# conflict until @tremor/react is replaced with React 19-compatible alternatives.
# See: https://github.com/tremorlabs/tremor/issues
legacy-peer-deps=true
